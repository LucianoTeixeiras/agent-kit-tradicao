[project]
name = "agent-kit-tradicao"
version = "0.1.0"
description = "Holding cultural: Doutrina e Personas para o Legado Tradicionalista Digital."
dependencies = [
    # Adicionar dependências aqui conforme o desenvolvimento
]
requires-python = ">=3.9"
authors = [
    {name = "Luciano Teixeira", email = "luciano.teixeiras@gmail.com"},
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
